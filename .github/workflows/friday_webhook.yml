name: Send Friday Webhook
on:
  workflow_dispatch: # 직접 실행 버튼을 생성함
  schedule:
    - cron: '0 7 * * 5' # 매주 금요일 한국 시간 오후 4시 (UTC 07:00)

jobs:
  send_webhook:
    runs-on: ubuntu-latest
    steps:
      - name: Send POST Request
        run: |
          curl -X POST "https://kakaowork.com/bots/hook/55818b68831347139dbb65199eee3031" \
          -H "Content-Type: application/json" \
          -d '{
 "text": "블록킷 메시지입니다.",
  "blocks": [
    {
      "type": "header",
      "text": "📢 **주간업무보고 작성 시간",
      "style": "white"
    },
    {
      "type": "text",
      "text": "text sample",
      "inlines": [
        {
          "type": "styled",
          "text": "금요일 오후 4시입니다~ ",
          "bold": false,
          "color": "default"
        }
      ]
    },
    {
      "type": "button",
      "text": "바로가",
      "style": "default",
      "action": {
        "type": "open_system_browser",
        "name": "button1",
        "value": "https://drive.google.com/drive/u/0/folders/1nmz8c-qtouAGZpgJAajvTsLujrS3mrpt?ths=true"
      }
    }
  ]
          }'
